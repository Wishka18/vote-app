{% extends 'base.html.twig' %}
{% block title %}Modifier les détails du candidat
{% endblock %}

{% block content %}
	<div class="crud-prefix-container">
		<h1 class="crud-prefix-title">Modifier les détails du candidat</h1>
        <br>
		{{ include('candidate/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
		<div class="crud-prefix-btn-group">
			<a href="{{ path('app_candidate_index') }}" class="crud-prefix-btn crud-prefix-btn-view" title="Retour à la liste">
				<i class="fas fa-arrow-left"></i>
			</a>
			{{ include('candidate/_delete_form.html.twig') }}
		</div>
	</div>
{% endblock %}
